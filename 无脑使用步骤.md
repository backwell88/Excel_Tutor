# 无脑使用步骤

> 这份文档只回答一件事：**我现在要怎么把 Excel Tutor 用起来？**

## 每次使用：三步

1. 在项目根目录打开 PowerShell。
2. 输入并回车：

   ```powershell
   npm.cmd run dev
   ```

3. 不要关闭这个 PowerShell 窗口。正常打开 Excel，在 `Excel Tutor` 选项卡点击 `Open Tutor`。

现在就可以提问了。

## 第一次使用：只多做一次

如果项目根目录还没有 `.env` 文件，先依次执行：

```powershell
Copy-Item .env.example .env
notepad .env
```

在打开的 `.env` 中填入你的 DeepSeek API Key，保存并关闭。然后执行：

```powershell
npm.cmd install
npm.cmd run certs:install
npm.cmd run dev
```

## 你只需要记住

- **想用 AI：** `npm.cmd run dev`
- **Excel Tutor 找不到或打不开：** 看 `部署阶段问题及相关说明.md`
- **不要做：** 不要把 `.env` 上传到 GitHub；不要在“开发工具 - XML 工具”中导入 `manifest.xml`

## 用完怎么办？

直接关闭 Excel 和运行 `npm.cmd run dev` 的 PowerShell 窗口即可。API Key 仍保留在本机 `.env`，下次不需要重新填写。